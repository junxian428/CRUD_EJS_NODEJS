<!DOCTYPE html>
<html>
<head>
  <title>Items</title>
</head>
<body>
  <h1>Items</h1>
  <ul>
    <% items.forEach(item => { %>
      <li>
        <a href="/item/<%= item.id %>">
          <%= item.name %>
        </a>
      </li>
    <%  }); %>
  </ul>
  <a href="/item/new">Add New Item</a>
</body>
</html>
